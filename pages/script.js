const menu = document.querySelector(".menu");
const hiddenBox = document.querySelector(".mob-nav");

menu.addEventListener('click', () => {
    //alert("am clicked now");
    hiddenBox.classList.display.ad("block");
});z